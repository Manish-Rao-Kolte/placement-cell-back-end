{
    "version" : 2,
    "builds": [
        { "src": "src/index.js", "use": "@vercel/node" }
    ],
    "routes": [
        { 
            "src": "/(.*)", 
            "dest": "/api/v1/",
            "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"] 
        }
    ]
}